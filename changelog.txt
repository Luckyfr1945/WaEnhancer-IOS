[ADDED]
* Video Call Screen Recording: Added automatic screen recording for WhatsApp video calls with background root capture and clean MP4 output
* Jump to First Message: Added menu option in conversation chats to instantly jump and navigate to the very first message
* WhatsApp ConversationScrollApi Hook: Hooked WhatsApp's internal ConversationScrollApi for smooth message list scrolling and dynamic history loading
* Sticker Sync & Backup: Added full backup (.zip) and restore/merge tools for WhatsApp & WhatsApp Business stickers
* Long-Press "Anda" Tab for Account Switcher: Long-pressing the "Anda"/Settings tab in the floating bottom navigation bar opens WhatsApp's native Account Switching bottom sheet

[IMPROVED]
* Adaptive Navigation Bar: Floating bottom nav bar automatically adapts width and hides unavailable tabs based on LSPosed and Root activation state
* Extended Video Size Limit: Increased maximum video size limit slider from 90MB up to 120MB in Media settings
* Account Switcher Gesture Recognition: Integrated low-latency (260ms) touch gesture recognition on the "Anda" tab with 1500ms debounce cooldown to prevent double popup triggers
* Obfuscation-Resilient Fragment & Dialog Invocation: Dynamic reflection fallback calling A4P/supportFragmentManager and safe bundle argument injection across WhatsApp updates
* Dynamic Status Badges: Real-time red/green indicator badges for LSPosed and Root status on the Hero Control Center
* Direct GitHub Update Redirection: Update dialog directly opens the latest GitHub APK release download in browser
* Native Voice Note & Send Buttons: Preserved native WhatsApp floating action button styling, colors, and animations for voice note and send buttons
* About & Contributors UI: Standardized all contributor pill buttons with consistent styling and uniform theme colors
* Performance & Clean Logging: Streamlined diagnostic logs across modules with zero-overhead logDebug

[FIXED]
* Fixed Jump to First Message Navigation: Replaced fragile reflection with direct ConversationScrollApi navigation, resolving issues where message scrolling did not trigger or caused unintended reply quotes
* Fixed Search Filtering: Filtered search catalog to hide unavailable hook-dependent preferences when LSPosed is not active
* Fixed Intermediate Tab Flickering: Created dynamic ViewPager adapter modes so transitions switch directly without passing through hidden tabs
* Fixed Per-Contact Privacy Overrides (HideSeen): Removed global toggle early-returns in hookReceiptMethod so individual contact privacy overrides are always respected even when global toggles are off
* Fixed Multi-Job Architecture Crash: Keyed cachedMessageIdsFields by Class in ConcurrentHashMap to prevent IllegalArgumentException across WhatsApp and WhatsApp Business
* Fixed False-Positive Read Receipts (SeenTick): Replaced fuzzy substring matching with normalized exact-match JID validation to prevent misdirected blue ticks and played receipts
* Fixed Profile Avatar Lookup: Added multi-package directory support for both WhatsApp Standard and WhatsApp Business profile avatars
* Fixed Account Switcher Double-Popup & Crash: Resolved NoSuchMethodError on obfuscated setArguments methods and prevented redundant synthetic touch dispatching
* Fixed Outgoing Message Pending Stall (🕒): Added strict stanza tag validation (`tag != "receipt"`) in HideSeen, preventing outgoing `<message>` stanzas from being corrupted with `type="inactive"`, and reinforced `extractIsFromMe` with resilient `Key.toString()` parsing
* Fixed SeenTick Job Queue Feedback Loop: Added `blue_on_reply` self-dispatch marker and `SendE2EMessageJob` type guards in `hookOnSendMessages` to eliminate recursive read-receipt job loops
* Fixed Anti-Revoke Native Replacement: Updated `setBlockedResult()` in AntiRevoke to return `false`/`null`, preventing WhatsApp from marking revoked messages as successfully deleted and replacing them with placeholder text
* Fixed Anti-Revoke Target Message ID Tracking: Captured original target message IDs, `originalKey.messageID`, and candidate target fields from revoke stanzas so deleted messages are accurately identified in the UI
* Fixed Anti-Revoke Multi-JID & LID Resolution: Unified canonical `MutableSet` mapping across all JID representations (`@lid`, `@s.whatsapp.net`, raw, and normalized phone numbers) to ensure consistent message lookup regardless of timing
* Fixed Conversation Item UI Rendering: Resolved `NoSuchMethodException` on `getDeclaredMethod("getView", ...)` by searching the full adapter inheritance hierarchy and added recursive `findDateTextView` fallback for message bubbles
* Fixed MessageStore Transaction Rollback & Data Integrity: Wrapped `db.transaction` with proper exception rethrowing to prevent partial-write commits on failed status deletions, and fixed `getAudioListByMessageList` column selection
* Fixed Media & Document Upload Stall on Hide Second Tick: Guarded `hookReceiptMethod` in HideSeen to only alter delivery receipt stanzas (`type == null` or `"delivery"`), preventing media/MMS negotiation receipts from being mutated to `"inactive"`
* Fixed Chat List False Account Switcher Trigger: Removed global `Activity.dispatchTouchEvent` hook and restricted long-press touch scope strictly to the physical bottom navigation bar's "Anda" tab
* Fixed Forwarded Media Visibility in Sender POV: Restricted `forwardAudioTypeMethod` origin field modification in Others to audio/voice note messages only (`mediaType == 2 || 9`), keeping forwarded photos and videos intact in chat view
